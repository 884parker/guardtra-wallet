// ConnectWalletButton removed — Pause Wallet does not use external wallet connections.
// All wallet operations are managed internally through the Pause Wallet security stack.

export default function ConnectWalletButton() {
  return null;
}
