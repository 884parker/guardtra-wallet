// WalletConnect/Reown has been intentionally removed from Guardtra.
// All transactions are programmatic — internal routing between Guardtra and GuardtraSafe.
// No external wallet connections = no external attack surface.

export default function WalletConnectConfig() {
  return null;
}
